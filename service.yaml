apiVersion: v1
kind: Service
metadata:
  name: glue_crawler
spec:
  selector:
    app: glue_crawler
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
